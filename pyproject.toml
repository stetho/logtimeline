[project]
name = "logtimeline"
version = "0.1.0"
description = "Merge heterogeneous log sources into one time-ordered stream"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
